<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.ParcelEyeLogin" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/aqua</item>
    </style>

    <style name="Theme.ParcelEyeLogin" parent="Base.Theme.ParcelEyeLogin" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo2_01</item>
        <item name="postSplashScreenTheme">@style/Theme.ParcelEyeLogin</item>
    </style>
</resources>